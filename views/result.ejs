<!DOCTYPE html>
<html>
  <head>
    <title>Shortened URL</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body
    class="bg-light d-flex justify-content-center align-items-center vh-100"
  >
    <div class="card shadow-lg p-5 text-center" style="width: 500px">
      <h2 class="mb-4">✅ Your Shortened URL</h2>
      <p>
        Original: <a href="<%= longUrl %>" target="_blank"><%= longUrl %></a>
      </p>
      <p>
        Short: <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
      </p>
      <a href="/" class="link-offset-2 link-underline link-underline-opacity-0">
        <button>Shorten Another</button>
      </a>
    </div>
  </body>
</html>
